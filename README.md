# proj
DBMS Mini Project on Donor Management System

Use XAMP for backend management.
Create a database titles KDBMS and import the kdbms.sql file provided.

Copy the proj directory(as is) to the htdocs directory of XAMP installation.
Since the html files have hard coded links to folder title 'proj' do not rename the folder.
